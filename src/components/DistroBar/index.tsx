import styles from "./styles.module.scss";

const DistroBar = () => {
    return (
        <div className={styles.distroBar}>
            distro bar
        </div>
    );
};

export default DistroBar;
