import styles from "./styles.module.scss";

const MusicButton = () => {
    return (
        <div className={styles.container}>
            music button here
        </div>
    );
};

export default MusicButton;
